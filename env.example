# ============================================
# FRONTEND CONFIGURATION
# ============================================
# This is a frontend-only project that connects to a separate backend API
# Backend should be running on a different port/server

# API base URL for the frontend to connect to the backend API
# Change this to match your backend server URL
# Example: http://localhost:3000 or https://api.yourdomain.com
VITE_API_URL=http://localhost:3000
